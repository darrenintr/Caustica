# 降噪器与非完整方块对齐 - 技术说明

## 问题描述

用户报告冰块和水面在降噪后产生严重的模糊/散射效果，看起来像磨砂玻璃。

## 根本原因

降噪器使用 `kSmoothDielectric` 阈值来识别"平滑电介质"（水/冰/玻璃），
对这些材质使用特殊的去模处理以避免 SPP-1 光线追踪的萤火虫伪影。

**原始值**: `kSmoothDielectric = 0.12`

这个阈值过高，导致以下问题：
- 水面粗糙度: 0.08（应该被特殊处理）✓
- 玻璃粗糙度: 0.10（应该被特殊处理）✓
- 但阈值 0.12 可能意外包含其他方块

## 修复方案

### 1. 降低阈值
将 `kSmoothDielectric` 从 0.12 降低到 **0.08**

```glsl
// prepare_nrd_inputs.comp
const float kSmoothDielectric = 0.08;

// nrd_compose_beauty.comp  
const float kSmoothDielectric = 0.08;
```

### 2. 材质粗糙度参考

| 材质类型 | 粗糙度 | 是否为平滑电介质 |
|---------|-------|----------------|
| 水 | 0.08 | ✓ (≤ 0.08) |
| 玻璃 | 0.10 | ✗ (> 0.08) |
| 冰 | ~0.10 | ✗ (> 0.08) |
| 金属 | 0.30 | ✗ |
| 平滑石头 | 0.40 | ✗ |
| 普通方块 | 0.95 | ✗ |

**注意**: 冰块和玻璃现在不再被当作"平滑电介质"特殊处理，
但它们的粗糙度仍然足够低，降噪器会正确处理其镜面反射。

### 3. 影响

- **水**: 继续使用特殊路径（预调制 beauty）
- **冰/玻璃**: 使用标准去模路径（demode + REBLUR + remodulate）
- **其他方块**: 不受影响

## 非完整方块

台阶、楼梯、栅栏等非完整方块使用与对应完整方块相同的材质属性：
- 石质台阶: 粗糙度 0.95（普通）
- 木质栅栏: 粗糙度 0.95（普通）  
- 玻璃板: 粗糙度 0.10（与玻璃块相同）

降噪器基于粗糙度值处理，与方块几何形状无关。

## 验证

重启游戏后检查：
1. 水面应该清澈，反射清晰
2. 冰地形应该透明，没有过度模糊
3. 玻璃方块应该正常透明
4. 台阶/楼梯等非完整方块应该与完整方块表现一致

## 配置调整

如果仍然觉得水面/冰块模糊，可以进一步调整降噪器参数（caustica.toml）：

```toml
[denoise]
    mode = "NRD"
    sigma-depth = 0.02      # 降低深度边缘模糊
    sigma-normal = 0.05     # 降低法线边缘模糊
    sigma-color = 0.2       # 降低颜色空间滤波
    temporal-max = 0.75     # 减少时间累积
```
